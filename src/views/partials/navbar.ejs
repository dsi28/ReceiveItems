<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Receiving</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent"> 
        <ul class="navbar-nav ml-auto">
            <li class="nav-item ">
                <a class="nav-link" href="/batches">Batches<span class="sr-only"></span></a>
            </li>
        <%if(!currentUser){%>
            <li class="nav-item ">
                <a class="nav-link" href="/auth/login">Login<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/auth/register">Sign Up</a>
            </li>
        <%}else{%>
            <%if(currentUser.role == 'admin'){%>
                <li class='nav-item'>
                    <a class='nav-link' href='/admin'>Admin Portal</a>
                </li>
            <%}%>
            <li class='nav-item'>
                <a class='nav-link' href='/users/<%=currentUser._id%>'><%=currentUser.username%></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/auth/logout">Log Out</a>
            </li>
        <%}%>
        </ul>
    </div>
</nav>
<div class='container'>
    <%if(error && error.length >0){%>
        <div class="alert alert-danger" role="alert"><%=error%></div>
    <%}else if(success && success.length >0){%>
        <div class="alert alert-success" role="alert"><%=success%></div>
    <%}%>
</div>
